# Notebooks (Optional)

Use this folder for exploratory Jupyter notebooks on the transport dataset:

```python
import pandas as pd
df = pd.read_csv("../data/raw/transport_trips.csv")
df.head()
```

Run the full automated pipeline from the project root with `py src/main.py`.
